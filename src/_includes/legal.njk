{% extends "base.njk" %}

{% block page_styles %}
  <style>
    /* Hero */
    .page-hero { background: var(--teal); padding: 56px 0 48px; }
    .page-hero h1 { font-family: var(--font-serif); font-size: clamp(2rem, 4vw, 2.8rem); font-weight: 700; color: #fff; margin-bottom: 12px; }
    .page-hero p { font-size: 1.05rem; color: rgba(255,255,255,.8); max-width: 600px; line-height: 1.7; }
    .breadcrumb { font-size: .8rem; color: rgba(255,255,255,.55); margin-bottom: 16px; }
    .breadcrumb a { color: rgba(255,255,255,.55); }
    .breadcrumb a:hover { color: #fff; }
    /* Legal content */
    .legal-body { max-width: 780px; margin: 0 auto; padding: 64px 0 80px; }
    .legal-body h2 { font-family: var(--font-serif); font-size: 1.4rem; font-weight: 700; color: var(--teal); margin: 40px 0 12px; padding-top: 40px; border-top: 1px solid var(--gray-light); }
    .legal-body h2:first-child { margin-top: 0; padding-top: 0; border-top: none; }
    .legal-body h3 { font-family: var(--font-serif); font-size: 1.1rem; font-weight: 600; color: var(--dark); margin: 24px 0 8px; }
    .legal-body p { font-size: .97rem; color: var(--gray); line-height: 1.8; margin-bottom: 14px; }
    .legal-body ul, .legal-body ol { margin: 10px 0 18px 0; display: flex; flex-direction: column; gap: 8px; padding-left: 0; list-style: none; }
    .legal-body ul li, .legal-body ol li { display: flex; align-items: flex-start; gap: 10px; font-size: .93rem; color: var(--dark); line-height: 1.65; }
    .legal-body ul li::before { content: '–'; color: var(--teal); font-weight: 700; flex-shrink: 0; margin-top: 2px; }
    .legal-body ol { counter-reset: legal-counter; }
    .legal-body ol li { counter-increment: legal-counter; }
    .legal-body ol li::before { content: counter(legal-counter); background: var(--teal-pale); color: var(--teal); font-size: .78rem; font-weight: 700; width: 24px; height: 24px; border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0; margin-top: 1px; }
    .legal-body strong { color: var(--dark); font-weight: 600; }
    .legal-body a { color: var(--teal); text-decoration: underline; }
    .legal-body a:hover { color: var(--teal-light); }
    .legal-body hr { border: none; border-top: 1px solid var(--gray-light); margin: 32px 0; }
    .legal-body blockquote { background: var(--teal-pale); border-left: 4px solid var(--teal); border-radius: 0 var(--radius) var(--radius) 0; padding: 16px 20px; margin: 20px 0; font-size: .93rem; color: var(--gray); }
    /* Legal-pagina overrides voor gedeelde footer */
    .footer__hours li::before,
    .footer__links li::before { display: none !important; }
  </style>
{% endblock %}

{% block content %}
<section class="page-hero">
  <div class="container">
    <p class="breadcrumb"><a href="/">Home</a> › {{ breadcrumb_label }}</p>
    <h1>{{ hero_title }}</h1>
    <p>{{ hero_subtitle }}</p>
  </div>
</section>

<main>
  <div class="container">
    <div class="legal-body">
      {{ content | safe }}
    </div>
  </div>
</main>
{% endblock %}
